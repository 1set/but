name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build lite version
      run: |
        cd lite
        make buildnew
    - name: Build full version
      run: |
        cd full
        make buildnew
